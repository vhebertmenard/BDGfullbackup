<?xml version="1.0" encoding="utf-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Branch_Name__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>Used to pull in Branch details for the Opportunity, e.g. Region (used to populate Division/Region), Branch Name Text (used for syncing)</description>
    <inlineHelpText>Select the branch responsible for fulfilling this Opportunity.</inlineHelpText>
    <label>Branch Name</label>
    <lookupFilter>
        <active>true</active>
        <filterItems>
            <field>Branch__c.Status__c</field>
            <operation>equals</operation>
            <value>Active</value>
        </filterItems>
        <isOptional>false</isOptional>
    </lookupFilter>
    <referenceTo>Branch__c</referenceTo>
    <relationshipLabel>Opportunities Related to this Branch</relationshipLabel>
    <relationshipName>Opportunities</relationshipName>
    <required>false</required>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>false</trackHistory>
    <type>Lookup</type>
</CustomField>
