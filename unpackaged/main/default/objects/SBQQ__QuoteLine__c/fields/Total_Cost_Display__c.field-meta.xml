<?xml version="1.0" encoding="utf-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Total_Cost_Display__c</fullName>
    <description>Used on Bid Sheet to display Total Cost, but for only on Unit lines (hiding from Product lines) to avoid duplicate totals</description>
    <formula>IF(AND(Shell__c = TRUE, SBQQ__ProductFamily__c ='VAPS'),Total_Cost__c,(IF(Shell__c = TRUE,NULL,Total_Cost__c)))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>Used on Bid Sheet to display Total Cost, but for only on Unit lines (hiding from Product lines) to avoid duplicate totals</inlineHelpText>
    <label>Total Cost Display</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Currency</type>
</CustomField>
